<div class="navbar navbar-inverse navbar-fixed-top ">
  <div class="container">
    <div class="navbar-header">
      <a class="button navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to(Setting.app_name, Setting.domain , :class => "navbar-brand" )%>
    </div>
    <%= render :partial => "common/search_form" %>
    <div class="navbar-collapse">
      <%= render_list :class => "nav navbar-nav" do |li|
        if not current_user.blank?
          li << link_to("All Spreadsheet", user_spreadsheets_path)
          li << link_to("Link-2","#")
          li << link_to("Link-3","#")
        end
      end %>  
      <%= render :partial => "common/user_nav"%>
    </div>
  </div>
  <%= yield :submenu %>
</div>
